<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.leyou.item.mapper.SpuMapper">

    <select id="selectByTitle" resultType="com.leyou.item.domain.Spu">

        select * from tb_spu where title like #{key} and saleable= #{saleable}

    </select>

    <insert id="insertSelective" parameterType="com.leyou.item.bo.SpuBo">

        <selectKey keyProperty="id" keyColumn="id" resultType="Long">
            select last_insert_id();
        </selectKey>

        insert into tb_spu (title,sub_title,cid1,cid2,cid3,brand_id,saleable,valid,create_time,last_update_time) values
        (#{title},#{subTitle},#{cid1},#{cid2},#{cid3},#{brandId},#{saleable},#{valid},#{createTime},#{lastUpdateTime})



    </insert>

</mapper>